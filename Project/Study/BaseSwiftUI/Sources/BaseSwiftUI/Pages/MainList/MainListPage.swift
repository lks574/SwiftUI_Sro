import Foundation
import SwiftUI

public struct MainListPage: View {

  public init() { }

  public var body: some View {
    VStack(spacing: .zero) {
      Text("MainListPage")
      Spacer()
    }
    .frame(maxWidth: .infinity, alignment: .center)
  }
}
